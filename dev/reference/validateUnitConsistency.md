# Check if panel columns are filled consistently

Check if panel columns are filled consistently

## Usage

``` r
validateUnitConsistency(configTablePlots)
```

## Arguments

- configTablePlots:

  \`data.table\` configuration table without header lines
