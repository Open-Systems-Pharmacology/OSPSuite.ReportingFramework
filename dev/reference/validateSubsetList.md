# validate Subset List

Validates the subset list against the provided data frame.

## Usage

``` r
validateSubsetList(subsetList, data)
```

## Arguments

- subsetList:

  A list containing subsets to validate.

- data:

  A data frame containing the columns to validate.
