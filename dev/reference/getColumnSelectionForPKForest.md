# Get Column Selection for PK Forest

Selects columns for the PK forest plot based on the provided data and
ratio mode.

## Usage

``` r
getColumnSelectionForPKForest(plotData, ratioMode)
```

## Arguments

- plotData:

  Data table containing plot data.

- ratioMode:

  Mode for ratio calculations.

## Value

A list of selected columns for the plot.
