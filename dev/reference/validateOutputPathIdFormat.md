# Validates output path ID format

Validates output path ID format

## Usage

``` r
validateOutputPathIdFormat(configTablePlots, column = "outputPathIds")
```

## Arguments

- configTablePlots:

  \`data.table\` configuration table without header lines
