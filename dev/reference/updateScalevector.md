# Update Scale Vectors

This function updates the scale vectors for simulated and observed data
based on the provided input.

## Usage

``` r
updateScalevector(scaleVectorsInput)
```

## Value

A list containing updated scale vectors for both simulated and observed
data. Each component of the list includes properties such as \`color\`,
\`fill\`, and \`shape\`.
