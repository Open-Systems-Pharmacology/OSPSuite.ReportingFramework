# Convert Excel file to List Structure

This function converts the content of an Excel sheet into a list
structure suitable for JSON serialization.

## Usage

``` r
excelToListStructure(df)
```

## Arguments

- df:

  A data frame representing the content of an Excel sheet.

## Value

A list structure ready for JSON serialization, containing column names
and rows.
