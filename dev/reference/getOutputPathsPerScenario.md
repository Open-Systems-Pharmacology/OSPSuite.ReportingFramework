# Get Output Paths per scenario

This function retrieves output paths for each scenario based on the
configuration table.

## Usage

``` r
getOutputPathsPerScenario(configTable, dtOutputPaths)
```

## Arguments

- configTable:

  A data frame containing the configuration data.

## Value

A list of output paths per scenario.
