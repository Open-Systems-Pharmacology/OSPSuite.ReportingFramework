# Validate Configuration Table for Plots

This function validates the structure and content of the configuration
table for plots.

## Usage

``` r
validateConfigTableForPlots(configTable, ...)
```

## Arguments

- configTable:

  A data.table containing the plot configuration to be validated.

## Value

NULL. The function will throw an error if the validation fails.
