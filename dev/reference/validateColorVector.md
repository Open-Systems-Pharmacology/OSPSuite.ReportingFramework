# Validate Color Vector

This function checks if the provided color vector meets the following
criteria: - It is named. - All values are valid color names.

## Usage

``` r
validateColorVector(colorVector)
```

## Arguments

- colorVector:

  A character vector of colors, which should have a length of 2.

## Value

Returns \`invisible()\` if the checks pass.
